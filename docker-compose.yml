services:
  calc:
    build:
      context: ./calc
      dockerfile: Dockerfile
    expose:
      - "8080"
    environment:
      - PORT=8080

  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    env_file:
      - .env
    depends_on:
      - calc
