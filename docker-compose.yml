services:
  calc:
    build:
      context: ./calc
      dockerfile: Dockerfile
    expose:
      - "8080"
    env_file:
      - .env

  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    env_file:
      - .env
    depends_on:
      - calc
